<% if (selectedImages && selectedImages.length > 0) { %>
    <% selectedImages.forEach(imageUrl => { %>
        <img src="<%= imageUrl %>" width="100" class="selected-img">
    <% }); %>
<% } else { %>
    <p>No images selected.</p>
<% } %>